version: '3'
services:
  sftp:
    build: .
    volumes:
      - ./users.conf:/etc/sftp/users.conf:ro
      - ./volume:/home
      - /etc/ssh/ssh_host_rsa_key:/etc/ssh/ssh_host_rsa_key
    ports:
      - "2222:22"
